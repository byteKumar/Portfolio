/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: '/portfolio',
    // assetPrefix: '/portfolio/',
    //output: 'export',
    unoptimized: true,
}

module.exports = nextConfig
